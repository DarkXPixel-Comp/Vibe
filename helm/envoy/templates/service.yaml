apiVersion: v1
kind: Service
metadata:
  name: envoy
spec:
  type: {{ .Values.service.type }}
  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: 8080
  - name: admin
    port: 9901
    targetPort: 9901
  selector:
    app: envoy